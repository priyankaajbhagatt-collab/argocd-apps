apiVersion: v1
kind: Service
metadata:
  name: test-commerce-cart-app-service
spec:
  type: ClusterIP
  selector:
    app: test-commerce-cart-app-service
  ports:
    - name: http
      port: 80
      targetPort: 80
