apiVersion: v1
kind: Service
metadata:
  name: v6commerce-cart-app-service
spec:
  type: ClusterIP
  selector:
    app: v6commerce-cart-app-service
  ports:
    - name: http
      port: 80
      targetPort: 80
