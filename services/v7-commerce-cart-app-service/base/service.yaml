apiVersion: v1
kind: Service
metadata:
  name: v7-commerce-cart-app-service
spec:
  type: ClusterIP
  selector:
    app: v7-commerce-cart-app-service
  ports:
    - name: http
      port: 80
      targetPort: 80
