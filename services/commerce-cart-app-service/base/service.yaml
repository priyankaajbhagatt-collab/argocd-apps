apiVersion: v1
kind: Service
metadata:
  name: commerce-cart-app-service
spec:
  type: ClusterIP
  selector:
    app: commerce-cart-app-service
  ports:
    - name: http
      port: 80
      targetPort: 80
