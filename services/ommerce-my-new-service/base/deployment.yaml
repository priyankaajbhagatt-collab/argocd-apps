# Demo placeholder Deployment — image is overridden per env by the
# overlay's `images:` block (which the service's deployspec.yml in turn
# rewrites via `argocd app set --kustomize-image` once a real image
# exists in ECR).
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ommerce-my-new-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ommerce-my-new-service
  template:
    metadata:
      labels:
        app: ommerce-my-new-service
    spec:
      containers:
        - name: ommerce-my-new-service
          image: public.ecr.aws/docker/library/nginx:alpine
          ports:
            - containerPort: 80
          readinessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 3
          livenessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 10
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 200m
              memory: 256Mi
