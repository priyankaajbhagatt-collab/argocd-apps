apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: test-scoring-service-usa
  namespace: argocd
  labels:
    service: test-scoring-service
    market: usa
    group: ai-insights
    managed-by: backstage-scaffolder
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default

  source:
    repoURL: https://github.com/priyankaajbhagatt-collab/test-scoring-service.git
    targetRevision: main
    path: deploy/overlays/usa

  destination:
    server: https://kubernetes.default.svc
    namespace: test-scoring-service-usa

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
    retry:
      limit: 3
      backoff:
        duration: 30s
        factor: 2
        maxDuration: 5m
